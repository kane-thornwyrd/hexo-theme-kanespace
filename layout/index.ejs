<% if (config.photos && !!config.photos.length){ %>
  <%- partial('_partial/post/gallery',{item: {photos: config.photos}}) %>
<% } %>


<% page.posts.each(function(item){ %>
  <%- partial('_partial/article', {item: item, index: true}) %>
<% }); %>
